{% extends 'base.html.twig' %}

{% block title %}Formulaire rendez-vous{% endblock %}

{% block body %}

    <div class="container register">
        <h1 class="h1-register">Prise de RDV</h1>

        {{ form_start(RDVForm) }}

            {{ form_row(RDVForm.praticien) }}     
            {{ form_row(RDVForm.day) }}
            {{ form_row(RDVForm.hour) }}
            {{ form_row(RDVForm.message) }}

            <button class="btn btn-login" type="submit">Valider</button>
            </br></br>
        {{ form_end(RDVForm) }}

    </div>
    
{% endblock %}
