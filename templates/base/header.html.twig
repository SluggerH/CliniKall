
<!-- Nav1-->
<div class="container nav-1" style="color: white">

    <nav class="navbar navbar-light bg-light">
    <a class="navbar-brand" href="#">
        <!-- Logo -->
        <a href="{{ path('accueil') }}"><img src="/images/Clinikall 150x40.svg" width="250" alt=""></a>
    </a>
    </nav>
    <!-- Boutons -->
    <nav class="navbar navbar-light bg-light">
        <form class="form-inline">
            <!-- Espace professionel -->
            <a class="nav-link" href="#">Espace professionel</a>
             <!-- petit bouton -->
            <div class="btn-nav">
                <a class="btn btn-sm btn-outline-secondary btn-compte" href="{{path('app_register')}}" type="button">Créer un compte</a> <!-- border bleu typo bleu -->

                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('admin') }}">Page Admin</a><!-- fond bleu typo blanche -->
                    {% else %}
                        <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('user') }}">Mon compte</a><!-- fond bleu typo blanche -->
                    {% endif %}
                    <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('app_logout') }}">Déconnexion</a><!-- fond bleu typo blanche -->
                {% else %}
                    <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('app_login') }}">Se connecter</a> <!-- fond bleu typo blanche -->
                {% endif %}

            </div>
             <!-- petit bouton Fin -->
        </form>
    </nav>
    <!-- Boutons Fin -->
</div>
<!-- Nav1 Fin-->

<!-- Nav2 -->
<div class="container nav-2" style="color: white">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('accueil') }}">Accueil <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('nos_praticiens') }}">Nos praticiens</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('a_propos') }}">Notre clinique</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Prise de Rendez-vous</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Votre calendrier</a>
            </li>
            </ul>
        </div>
    </nav>
<div>
<!-- Nav2 Fin -->

