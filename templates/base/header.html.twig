<!-- Nav mobile -->
<div class="nav-mobile">
    <div class="nav-mobile-top">
        <div class="nav-brand-mobile">
            <a class="navbar-brand" href="{{ path('accueil') }}"><img src="/images/Clinikall 150x40.svg" width="170" height="50" class="d-inline-block align-top" alt="" loading="lazy">
            </a>
        </div>
    </div>
    


<<<<<<< HEAD
            <!-- burger -->
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
            
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <!-- burger fin -->

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                    
                        <li class="nav-item" >
                            <a class="nav-link {{ ROUTE_NAME == 'accueil' ? 'active' }}" href="{{ path('accueil') }}">Accueil <span class="sr-only">(current)</span></a>
                        </li>
                        <li class="nav-item" >
                            <a class="nav-link {{ ROUTE_NAME == 'nos_praticiens' ? 'active' }}" href="{{ path('nos_praticiens') }}">Nos praticiens</a>
                        </li>
                        <li class="nav-item" >
                            <a class="nav-link {{ ROUTE_NAME == 'a_propos' ? 'active' }}" href="{{ path('a_propos') }}">Notre clinique</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('reservation_praticiens') }}">Prise de Rendez-vous</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Mes rendez-vous</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-sm btn-outline-secondary btn-connzexion" type="button" href="{{ path('admin') }}">Page Admin</a>
                        </li>
                    </ul>
                    
                    {% if app.user %}
=======
    <nav class="navbar navbar-light bg-light">
    <a class="navbar-brand" href="#">
        <!-- Logo -->
        <a href="{{ path('accueil') }}"><img src="/images/Clinikall 150x40.svg" width="250" alt=""></a>
    </a>
    </nav>
    <!-- Boutons -->
    <nav class="navbar navbar-light bg-light">
        <form class="form-inline">
            <!-- Espace professionel -->
            <a class="nav-link" href="{{path('register_pro')}}">Inscription professionel</a>
             <!-- petit bouton -->
            <div class="btn-nav">
                <a class="btn btn-sm btn-outline-secondary btn-compte" href="{{path('app_register')}}" type="button">Créer un compte</a> <!-- border bleu typo bleu -->

                {% if app.user %}
>>>>>>> e4592775b0f9e5abf332a35fdd7d93c0dd009dfd
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="btn btn-sm btn-outline-secondary btn-connzexion" type="button" href="{{ path('admin') }}">Page Admin</a>
                    {% else %}
                        <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('user') }}">Mon compte</a>
                    {% endif %}
                    <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('app_logout') }}">Déconnexion</a>
                    {% else %}
                        <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('app_login') }}">Se connecter</a> 
                    {% endif %}
                </div>
            </nav>
        </nav>
    </div>    
</div>
<<<<<<< HEAD
<!-- Fin Nav mobile -->



<!-- Nav ordi -->
<div class="nav-ordi">

    <!-- Nav 1 ordi -->
    <div class="container identification">
        <nav class="navbar navbar-light bg-light">
        <a class="navbar-brand" href="#">
        <!-- Logo -->
            <a href="{{ path('accueil') }}"><img src="/images/Clinikall 150x40.svg" width="250" alt=""></a>
        </a>
        </nav>
        <!-- Boutons -->
        <nav class="navbar navbar-light bg-light">
            <form class="form-inline">
                <!-- Espace professionel -->
                <a class="nav-link" href="{{path('register_pro')}}">Inscription professionnelle</a>
                <!-- petit bouton -->
                <div class="btn-nav">
                    <a class="btn btn-sm btn-outline-secondary btn-compte" href="{{path('app_register')}}" type="button">Créer un compte</a> 

                    {% if app.user %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('admin') }}">Page Admin</a>
                        {% else %}
                            <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('user') }}">Mon compte</a>
                        {% endif %}
                        <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('app_logout') }}">Déconnexion</a>
                    {% else %}
                        <a class="btn btn-sm btn-outline-secondary btn-connexion" type="button" href="{{ path('app_login') }}">Se connecter</a> 
                    {% endif %}
                </div>
            </form>
        </nav>
    </div>
    <!-- Nav1 Fin-->

    <!-- Nav2 -->
    <div class="container navigation" style="color: white">
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            
            {# <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button> #}
             <div class="collapse navbar-collapse nav-ordi" id="navbarNav">
                <ul class="navbar-nav">
                <li class="nav-item" >
                    <a class="nav-link {{ ROUTE_NAME == 'accueil' ? 'active' }}" href="{{ path('accueil') }}">Accueil <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item" >
                    <a class="nav-link {{ ROUTE_NAME == 'nos_praticiens' ? 'active' }}" href="{{ path('nos_praticiens') }}">Nos praticiens</a>
                </li>
                <li class="nav-item" >
                    <a class="nav-link {{ ROUTE_NAME == 'a_propos' ? 'active' }}" href="{{ path('a_propos') }}">Notre clinique</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('reservation_praticiens') }}">Prise de Rendez-vous</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Mes rendez-vous</a>
                </li>
                </ul>
            </div>
        </nav>
    </div>
    <!-- Nav2 Fin -->
=======
<!-- Nav1 Fin-->

<!-- Nav2 -->
<div class="container nav-2" style="color: white">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
            <li class="nav-item" >
                <a class="nav-link {{ ROUTE_NAME == 'accueil' ? 'active' }}" href="{{ path('accueil') }}">Accueil <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item" >
                <a class="nav-link {{ ROUTE_NAME == 'nos_praticiens' ? 'active' }}" href="{{ path('nos_praticiens') }}">Nos praticiens</a>
            </li>
            <li class="nav-item" >
                <a class="nav-link {{ ROUTE_NAME == 'a_propos' ? 'active' }}" href="{{ path('a_propos') }}">Notre clinique</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {{ ROUTE_NAME == 'reservation_praticiens' ? 'active' }}" href="{{ path('reservation_praticiens') }}">Prise de Rendez-vous</a>
            </li>

            {% if app.user %}
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Mes rdv</a>
                    </li>
                {% elseif is_granted('ROLE_PRO') %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Mes rdv</a>
                    </li>
                {% endif %}
                
            {% endif %}
            
            </ul>
        </div>
    </nav>
>>>>>>> e4592775b0f9e5abf332a35fdd7d93c0dd009dfd
<div>
<!-- Fin nav ordi -->



    






